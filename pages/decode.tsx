import WithAuth from "@/components/WithAuth";
import AllSections from "@/components/AllSections";

const Decode = () => {
  return <AllSections />;
};

export default Decode; //  WithAuth( ) is a HOC that wraps the component and checks if the user is logged in
